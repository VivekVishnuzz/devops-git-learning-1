#these are the basic git coomands

git.init means creating a repo
git.status to check status of file
git.add to tell git whether u should keep an eye on it
git coomit to save the snapshot
git remote add origin <repo url> to connect to github
we can use git checkout to switch between branches if we use -b it will create a new branch 
eg: git checkout -b learning creates a new branch named learning
and we will be doing the works in branch now 

for merging we csn use either cherrypick,merge, or rebase 
	cherry pick is easy but diidficult for large projects as we need to  chooose the commit id 
 if we use merge it will be pushed on to top so hard for linear loggging
if we use rebasse it folows linear looging

